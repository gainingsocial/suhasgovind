node_modules/
dist/
build/
coverage/
.turbo/
.next/
.open-next/
.wrangler/
*.tsbuildinfo

# Secrets — never commit. Provider client secrets, KEKs and webhook signing
# roots live in Cloudflare Secrets Store (plan §6.6, §7).
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Local tooling
.DS_Store
Thumbs.db
*.log
npm-debug.log*
pnpm-debug.log*
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# Generated artifacts that are committed deliberately live in docs/ and
# packages/contracts/openapi — everything else generated is ignored.
tmp/
.scratch/

# Next.js / OpenNext build output
apps/web/out/
apps/web/.open-next/
